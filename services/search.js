module.exports = {
    /**
     * needs work
     * @param {*} req 
     * @param {*} res 
     * @returns http status code
     */
    delete(req, res) {
        
    },

    /**
     * WHY IS TENURE == Roommate ??? can't we do better ??
     * @param {*} req 
     * @param {*} res 
     * @returns 
     */
    save(req, res, next) {
        
    },

    /**
     * needs work
     * @param {*} req 
     * @param {*} res 
     * @returns 
     */

    search(req, res) {

        
      
      
    },
}